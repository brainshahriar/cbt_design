import Sidebar from "../../components/organism/sidebar";

const index:React.FC= () => {
    return (
        <div className="body">
            <Sidebar/>
        </div>
    );
};

export default index;